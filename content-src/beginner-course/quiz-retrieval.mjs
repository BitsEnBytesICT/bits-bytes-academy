import { P, B } from "./quiz-authoring.mjs";
// Deliberate recall pairs: related prerequisites, changed contexts and different
// boundary cases on retry. These are not copies of earlier quiz questions.
export function recall(chapter, alt) {
  const pairs = {
    2: [
      "execution-order",
      alt
        ? 'print("Open")\n# print("Closed")\nprint("Ready")'
        : 'print("Ready")\nprint("Open")',
      alt ? "Open\nReady" : "Ready\nOpen",
      ["Ready Open", "Closed\nReady"],
      "Comments are skipped; each executed print adds one line.",
      "Commentaar wordt overgeslagen; elke uitgevoerde print voegt één regel toe.",
    ],
    3: [
      "reassignment",
      `stock = ${alt ? 0 : 4}\nstock = stock + 2\nprint(stock)`,
      alt ? "2" : "6",
      ["4", "0"],
      "The right side uses the old value; assignment stores the new one.",
      "Rechts gebruikt de oude waarde; toewijzing bewaart de nieuwe.",
    ],
    4: [
      "precedence",
      alt ? "print((2 + 3) * 4)" : "print(2 + 3 * 4)",
      alt ? "20" : "14",
      [alt ? "14" : "20", "24"],
      "Parentheses group first; without them multiplication comes before addition.",
      "Haakjes groeperen eerst; zonder haakjes komt vermenigvuldigen vóór optellen.",
    ],
    5: [
      "str-conversion",
      `print("Room " + str(${alt ? -2 : 0}))`,
      alt ? "Room -2" : "Room 0",
      ["TypeError", "Room str"],
      "str makes number text suitable for concatenation.",
      "str maakt getaltekst die je kunt samenvoegen.",
    ],
    6: [
      "normalisation",
      alt
        ? 'text = " GO NOW "\nprint(text.strip().lower())'
        : 'text = " Quit "\nprint(text.lower())',
      alt ? "go now" : " quit ",
      ["gonow", "QUIT"],
      "lower changes case; only strip removes outer whitespace. Neither removes internal spaces.",
      "lower verandert hoofdletters; alleen strip verwijdert randwitruimte. Geen van beide verwijdert binnenspaties.",
    ],
    7: [
      "and",
      `age = ${alt ? 12 : 13}\nallowed = age >= 12 and age < 13\nprint(allowed)`,
      alt ? "True" : "False",
      [alt ? "False" : "True", "12"],
      "Both comparisons must hold, including the strict upper boundary.",
      "Beide vergelijkingen moeten kloppen, inclusief de strikte bovengrens.",
    ],
    8: [
      "elif",
      `score = ${alt ? 0 : 10}\nif score >= 10:\n    print("Gold")\nelif score >= 0:\n    print("Entered")`,
      alt ? "Entered" : "Gold",
      ["Gold\nEntered", "Nothing"],
      "An if/elif chain selects only its first matching branch.",
      "Een if/elif-keten kiest alleen de eerste passende tak.",
    ],
    9: [
      "while",
      `count = ${alt ? 2 : 0}\nwhile count < 2:\n    count += 1\nprint(count)`,
      "2",
      ["0", "3"],
      alt
        ? "The condition is false on entry, so the loop body is skipped."
        : "The body updates count to 1 and then 2 before the condition fails.",
      alt
        ? "De voorwaarde is bij binnenkomst onwaar, dus het lusblok wordt overgeslagen."
        : "Het blok verhoogt count tot 1 en daarna 2 vóór de voorwaarde onwaar wordt.",
    ],
    10: [
      "default-arguments",
      `def cost(count, price=3):\n    return count * price\nprint(cost(${alt ? "price=2, count=4" : "0"}))`,
      alt ? "8" : "0",
      ["12", "3"],
      "Keyword names bind arguments explicitly; an omitted price uses the default, and zero remains a value.",
      "Benoemde argumenten worden expliciet gekoppeld; een ontbrekende prijs gebruikt de standaard en nul blijft een waarde.",
    ],
    11: [
      "none",
      `def read(text):\n    try:\n        return int(text)\n    except ValueError:\n        return None\nprint(read("${alt ? "bad" : "0"}") is None)`,
      alt ? "True" : "False",
      [alt ? "False" : "True", "ValueError"],
      "Invalid text returns None; converting zero succeeds and does not mean cancellation.",
      "Ongeldige tekst geeft None; nul omzetten slaagt en betekent geen annulering.",
    ],
    12: [
      "append",
      `items = [4]\nitems.append(${alt ? "0" : "4"})\nprint(len(items))`,
      "2",
      ["1", "4"],
      "append adds one element even when that element is zero or duplicates an existing one.",
      "append voegt één element toe, ook nul of een al aanwezige waarde.",
    ],
    13: [
      "range",
      `print(list(range(${alt ? "3, 0, -1" : "0"})))`,
      alt ? "[3, 2, 1]" : "[]",
      ["[0]", "[3, 2, 1, 0]"],
      "The stop is excluded. range(0) is empty; a negative step counts downward.",
      "De eindgrens telt niet mee. range(0) is leeg; een negatieve stap telt omlaag.",
    ],
    14: [
      "packing-unpacking",
      `def pair():\n    return 9, 4\nx, y = pair()\nprint(${alt ? "y - x" : "x - y"})`,
      alt ? "-5" : "5",
      ["13", alt ? "5" : "-5"],
      "Unpacking binds x to the first returned value and y to the second.",
      "Uitpakken koppelt x aan de eerste teruggegeven waarde en y aan de tweede.",
    ],
    15: [
      "module-scope",
      `import math as geometry\nprint(geometry.floor(${alt ? "-1.2" : "1.2"}))`,
      alt ? "-2" : "1",
      ["0", alt ? "-1" : "2"],
      "The alias names the module; floor rounds toward negative infinity.",
      "De alias benoemt de module; floor rondt af richting min oneindig.",
    ],
    16: [
      "rect",
      `import pygame\npaddle = pygame.Rect(10, 20, 8, 40)\npaddle.y += ${alt ? "-5" : "5"}\nprint(paddle.bottom)`,
      alt ? "55" : "65",
      ["40", alt ? "65" : "55"],
      "bottom is y plus height; moving y changes bottom without changing height.",
      "bottom is y plus hoogte; y verplaatsen verandert bottom zonder de hoogte te veranderen.",
    ],
    17: [
      "serving",
      `vx = ${alt ? 120 : 0}\npressed = True\nif vx == 0 and pressed:\n    vx = 200\nprint(vx)`,
      alt ? "120" : "200",
      ["0", alt ? "200" : "120"],
      "A serve starts only a stopped ball; pressing during play preserves velocity.",
      "Een service start alleen een gestopte bal; indrukken tijdens spelen behoudt de snelheid.",
    ],
    18: [
      "sort",
      `values = [3, 1]\n${alt ? "copy = sorted(values)\nprint(values)" : "result = values.sort()\nprint(result)"}`,
      alt ? "[3, 1]" : "None",
      ["[1, 3]", alt ? "None" : "[3, 1]"],
      "sorted returns a new list; sort mutates its list and returns None.",
      "sorted geeft een nieuwe lijst; sort wijzigt zijn lijst en geeft None.",
    ],
    19: [
      "split-delimiters",
      `print("${alt ? ",a," : "a,,b"}".split(","))`,
      alt ? "['', 'a', '']" : "['a', '', 'b']",
      ["['a', 'b']", "['a']"],
      "An explicit delimiter preserves empty fields, including at either end.",
      "Een expliciet scheidingsteken bewaart lege velden, ook aan beide uiteinden.",
    ],
    20: [
      "get",
      `record = {"count":0}\nprint(record.get("${alt ? "missing" : "count"}", 7))`,
      alt ? "7" : "0",
      [alt ? "0" : "7", "KeyError"],
      "get uses the default only when the key is missing, not when its value is zero.",
      "get gebruikt de standaard alleen als de sleutel ontbreekt, niet als de waarde nul is.",
    ],
    21: [
      "file-position",
      `import io\nhandle = io.StringIO("one\\ntwo\\n")\n${alt ? "handle.read()" : "handle.readline()"}\nprint(repr(handle.read()))`,
      alt ? "''" : "'two\\n'",
      ["'one\\ntwo\\n'", alt ? "'two\\n'" : "''"],
      "Reading advances the cursor. A later read starts from the current position.",
      "Lezen verplaatst de cursor. Later lezen begint op de huidige positie.",
    ],
    22: [
      "csv-numeric-conversion",
      `row = {"score":"${alt ? "-2" : "0"}"}\nprint(int(row["score"]) + 1)`,
      alt ? "-1" : "1",
      [alt ? "-21" : "01", "TypeError"],
      "CSV fields start as strings. Convert the field before numeric addition.",
      "CSV-velden beginnen als strings. Zet het veld om vóór getalsoptelling.",
    ],
    23: [
      "json",
      `import json\nvalue = json.loads('${alt ? "null" : "false"}')\nprint(${alt ? "value is None" : "type(value).__name__"})`,
      alt ? "True" : "bool",
      [alt ? "False" : "str", "null"],
      "JSON null becomes None and JSON false becomes a Python Boolean.",
      "JSON null wordt None en JSON false wordt een Pythonboolean.",
    ],
  };
  const r = pairs[chapter];
  const p = P(
    r[0],
    r[1],
    [r[2], ...r[3]],
    [
      [r[4], r[5]],
      [r[4], r[5]],
      [r[4], r[5]],
    ],
  );
  const completions = {
    3: [
      "variables",
      "stock = 6\nprint(___)",
      "stock",
      ['"stock"', "Stock"],
      "6\n",
    ],
    4: [
      "modulo",
      `print(${alt ? 12 : 14} ___ 4)`,
      "%",
      ["/", "*"],
      `${alt ? 0 : 2}\n`,
    ],
    5: [
      "f-strings",
      'name = "Jo"\nprint(___)',
      'f"Hi {name}"',
      ['"Hi {name}"', "name"],
      "Hi Jo\n",
    ],
    6: [
      "int-conversion",
      `text = "${alt ? "-3" : "3"}"\nprint(___(text) + 1)`,
      "int",
      ["str", "len"],
      `${alt ? -2 : 4}\n`,
    ],
    7: [
      "not",
      `closed = ${alt ? "True" : "False"}\nprint(___ closed)`,
      "not",
      ["bool", "and"],
      `${alt ? "False" : "True"}\n`,
    ],
    8: [
      "inclusive-comparisons",
      `age = 12\nif age ___ 12:\n    print("Allowed")`,
      alt ? "==" : ">=",
      [">", "<"],
      "Allowed\n",
    ],
    9: [
      "while",
      "count = 0\nwhile count < 2:\n    count += 1\nprint(___)",
      "count",
      ["count + 1", "0"],
      "2\n",
    ],
    10: [
      "return",
      `def twice(value):\n    ___ value * 2\nprint(twice(${alt ? "-1" : "0"}))`,
      "return",
      ["print", "pass"],
      `${alt ? -2 : 0}\n`,
    ],
    11: [
      "value-error",
      'try:\n    int("blue")\nexcept ___:\n    print("Retry")',
      "ValueError",
      ["ZeroDivisionError", "NameError"],
      "Retry\n",
    ],
    12: [
      "negative-indexing",
      "items = [5, 8, 2]\nprint(items[___])",
      alt ? "-2" : "-1",
      alt ? ["0", "-1"] : ["0", "-2"],
      `${alt ? 8 : 2}\n`,
    ],
    13: [
      "for",
      "total = 0\nfor value in [2, 4]:\n    total ___ value\nprint(total)",
      "+=",
      ["=", "-="],
      "6\n",
    ],
    14: [
      "multiple-returns",
      "def pair():\n    return 3, 8\nx, y = ___\nprint(x + y)",
      "pair()",
      ["pair", "(3, 3)"],
      "11\n",
    ],
    15: [
      "selected-imports",
      "from math import floor\nprint(___(2.7))",
      "floor",
      ["math.floor", "floor.floor"],
      "2\n",
    ],
    16: [
      "rect",
      "import pygame\nr = pygame.Rect(3, 4, 10, 20)\nprint(r.___)",
      alt ? "bottom" : "right",
      alt ? ["y", "height"] : ["x", "width"],
      `${alt ? 24 : 13}\n`,
    ],
    17: [
      "velocity",
      "x = 10\nvx = -20\ndt = 0.1\nx += ___\nprint(x)",
      "vx * dt",
      ["vx", "dt"],
      "8.0\n",
    ],
    18: [
      "slicing",
      "items = [3, 6, 9]\nprint(items[___])",
      alt ? "1:" : ":2",
      alt ? [":1", ":2"] : ["1:", ":1"],
      alt ? "[6, 9]\n" : "[3, 6]\n",
    ],
    19: [
      "join",
      'parts = ["red", "blue"]\nprint("/".___(parts))',
      "join",
      ["split", "strip"],
      "red/blue\n",
    ],
    20: [
      "items",
      'record = {"Jo":0}\nfor name, value in record.___():\n    print(name, value)',
      "items",
      ["keys", "values"],
      "Jo 0\n",
    ],
    21: [
      "readline",
      'import io\nhandle = io.StringIO("a\\nb\\n")\nprint(repr(handle.___()))',
      "readline",
      ["read", "readlines"],
      "'a\\n'\n",
    ],
    22: [
      "csv-quoting",
      `import csv\nrow = next(csv.reader(['${alt ? "Bo" : "Jo"},"A,B"']))\nprint(len(row))\nprint(row[___])`,
      "1",
      ["0", "2"],
      "2\nA,B\n",
    ],
    23: [
      "json-save",
      'import json\ntext = json.dumps({"ready":True})\nprint(json.___(text)["ready"])',
      "loads",
      ["load", "dumps"],
      "True\n",
    ],
  };
  const c = completions[chapter];
  return {
    p,
    b:
      c &&
      B(
        c[0],
        c[1],
        c[2],
        c[3],
        [
          "Follow the operation and its input type, then compare the exact result shown.",
          "Volg de bewerking en het invoertype en vergelijk daarna het precieze getoonde resultaat.",
        ],
        c[4],
      ),
  };
}
