import { challenge as C } from "./helpers.mjs";
const ch = 12;
C(
  ch,
  "string-analysis",
  1,
  "Distinct symbols",
  "Verschillende tekens",
  "Return the number of different characters in text. Treat uppercase and lowercase as different characters.",
  "Geef het aantal verschillende tekens in text terug. Hoofdletters en kleine letters tellen als verschillende tekens.",
  "distinct_count(text)",
  "seen = []\nfor character in text:\n    if character not in seen:\n        seen.append(character)\nreturn len(seen)",
  "distinct_count('banana') == 3 and distinct_count('AaA') == 2 and distinct_count('') == 0",
  'print(distinct_count("banana"))',
);
C(
  ch,
  "string-analysis",
  2,
  "Find a marker",
  "Een markering tellen",
  "Count how often marker occurs in text. Assume marker is one character.",
  "Tel hoe vaak marker voorkomt in text. Neem aan dat marker één teken is.",
  "marker_count(text, marker)",
  "return text.count(marker)",
  "marker_count('a-b-c','-') == 2 and marker_count('abc','x') == 0",
  'print(marker_count("a-b-c", "-"))',
);
C(
  ch,
  "string-analysis",
  3,
  "Count a token",
  "Een token tellen",
  "Return the number of non-overlapping occurrences of a nonempty token in text.",
  "Geef het aantal niet-overlappende voorkomens van een niet-lege token in text terug.",
  "token_count(text, token)",
  "return text.count(token)",
  "token_count('aaaa','aa') == 2 and token_count('abababa','aba') == 2 and token_count('abc','z') == 0",
  'print(token_count("aaaa", "aa"))',
);
C(
  ch,
  "string-analysis",
  4,
  "Inside brackets",
  "Tussen haakjes",
  "Return the text between the first [ and the next ]. Return an empty string if either delimiter is missing or in the wrong order.",
  "Geef de tekst tussen de eerste [ en de volgende ] terug. Geef een lege string als een grens ontbreekt of de volgorde niet klopt.",
  "inside_brackets(text)",
  'start = text.find("[")\nif start == -1:\n    return ""\nend = text.find("]", start + 1)\nif end == -1:\n    return ""\nreturn text[start + 1:end]',
  "inside_brackets('x[blue]y') == 'blue' and inside_brackets('none') == '' and inside_brackets(']x[') == ''",
  'print(inside_brackets("x[blue]y"))',
);
C(
  ch,
  "string-analysis",
  5,
  "Short words only",
  "Alleen korte woorden",
  "Return True when every whitespace-separated word is at most limit characters long. An empty sentence returns True.",
  "Geef True als elk woord, gescheiden door whitespace, hoogstens limit tekens heeft. Een lege zin geeft True.",
  "short_words(sentence, limit)",
  "for word in sentence.split():\n    if len(word) > limit:\n        return False\nreturn True",
  "short_words('one two',3) is True and short_words('one three',3) is False and short_words('',3) is True",
  'print(short_words("one two", 3))',
);
C(
  ch,
  "string-transform",
  1,
  "Normalize a comparison",
  "Een vergelijking normaliseren",
  "Return True when two names match after trimming whitespace and converting to lowercase.",
  "Geef True als twee namen gelijk zijn na het verwijderen van whitespace aan de randen en omzetten naar kleine letters.",
  "same_label(left, right)",
  "return left.strip().lower() == right.strip().lower()",
  "same_label(' East ','east') is True and same_label('East','West') is False",
  'print(same_label(" East ", "east"))',
);
C(
  ch,
  "string-transform",
  2,
  "Sample a string",
  "Een string samplen",
  "Return the characters at even indexes: 0, 2, 4, and so on.",
  "Geef de tekens op even indexes terug: 0, 2, 4 enzovoort.",
  "sample_text(text)",
  "return text[::2]",
  "sample_text('abcdef') == 'ace' and sample_text('') == '' and sample_text('A') == 'A'",
  'print(sample_text("abcdef"))',
);
C(
  ch,
  "string-transform",
  3,
  "Reverse a label",
  "Een label omkeren",
  "Return text in reverse character order without changing its case or spaces.",
  "Geef text terug met de tekens in omgekeerde volgorde, zonder hoofdletters of spaties te veranderen.",
  "reverse_label(text)",
  "return text[::-1]",
  "reverse_label('Ab c') == 'c bA' and reverse_label('') == ''",
  'print(reverse_label("Ab c"))',
);
C(
  ch,
  "string-transform",
  4,
  "Exchange endings",
  "Uiteinden uitwisselen",
  "For two nonempty words, return a tuple with their final characters exchanged. Keep all earlier characters unchanged.",
  "Geef voor twee niet-lege woorden een tuple terug waarin de laatste tekens zijn uitgewisseld. Behoud alle eerdere tekens.",
  "swap_endings(left, right)",
  "return left[:-1] + right[-1], right[:-1] + left[-1]",
  "swap_endings('cat','dog') == ('cag','dot') and swap_endings('a','b') == ('b','a')",
  'print(swap_endings("cat", "dog"))',
);
C(
  ch,
  "string-transform",
  5,
  "Pad a display",
  "Een weergave opvullen",
  "Append dots until text reaches width characters. Do not truncate text that is already long enough.",
  "Voeg punten toe totdat text width tekens lang is. Kort tekst die al lang genoeg is niet in.",
  "pad_display(text, width)",
  'while len(text) < width:\n    text += "."\nreturn text',
  "pad_display('go',5) == 'go...' and pad_display('ready',3) == 'ready' and pad_display('',0) == ''",
  'print(pad_display("go", 5))',
);
C(
  ch,
  "mapping-total",
  1,
  "Sum a mapping",
  "Een mapping optellen",
  "Return the sum of all numeric dictionary values. Empty input returns 0.",
  "Geef de som van alle numerieke dictionary-values terug. Lege invoer geeft 0.",
  "sum_mapping(mapping)",
  "return sum(mapping.values())",
  "sum_mapping({'a':3,'b':7}) == 10 and sum_mapping({}) == 0",
  'print(sum_mapping({"a": 3, "b": 7}))',
);
C(
  ch,
  "mapping-total",
  2,
  "Select odd keys",
  "Oneven keys selecteren",
  "For integer keys and numeric values, return the sum of values whose keys are odd.",
  "Geef bij integer-keys en numerieke values de som terug van values met een oneven key.",
  "odd_key_total(mapping)",
  "total = 0\nfor key, value in mapping.items():\n    if key % 2 != 0:\n        total += value\nreturn total",
  "odd_key_total({1:4,2:9,3:6}) == 10 and odd_key_total({}) == 0",
  "print(odd_key_total({1: 4, 2: 9, 3: 6}))",
);
C(
  ch,
  "mapping-total",
  3,
  "Adjust every reading",
  "Elke meting bijstellen",
  "Return a new dictionary with offset added to every value. Leave the original unchanged.",
  "Geef een nieuwe dictionary terug waarin offset bij elke value is opgeteld. Laat het origineel ongewijzigd.",
  "adjust(mapping, offset)",
  "return {key: value + offset for key, value in mapping.items()}",
  "adjust({'a':2,'b':5},3) == {'a':5,'b':8} and (lambda d: (adjust(d,4),d)[1])({'a':2}) == {'a':2}",
  'print(adjust({"a": 2, "b": 5}, 3))',
);
C(
  ch,
  "mapping-total",
  4,
  "Linked labels",
  "Gekoppelde labels",
  "Return a list of values that also appear as keys, preserving value iteration order. Assume all values are strings.",
  "Geef een list terug met values die ook als key voorkomen, in de volgorde van de values. Neem aan dat alle values strings zijn.",
  "linked_values(mapping)",
  "return [value for value in mapping.values() if value in mapping]",
  "linked_values({'a':'b','b':'z','c':'a'}) == ['b','a'] and linked_values({}) == []",
  'print(linked_values({"a": "b", "b": "z", "c": "a"}))',
);
C(
  ch,
  "mapping-total",
  5,
  "Largest entry",
  "Grootste entry",
  "Return the key with the largest numeric value. On a tie return the first inserted key. Return None for an empty mapping.",
  "Geef de key met de grootste numerieke value terug. Bij gelijkheid kies je de eerst ingevoegde key. Geef None voor een lege mapping.",
  "largest_key(mapping)",
  "best_key = None\nfor key in mapping:\n    if best_key is None or mapping[key] > mapping[best_key]:\n        best_key = key\nreturn best_key",
  "largest_key({'a':2,'b':8,'c':8}) == 'b' and largest_key({}) is None and largest_key({'a':-9,'b':-3}) == 'b'",
  'print(largest_key({"a": 2, "b": 8, "c": 8}))',
);
C(
  ch,
  "mapping-build",
  1,
  "Word lengths",
  "Woordlengtes",
  "Return a dictionary mapping each input word to its character count. Repeated words need only one entry.",
  "Geef een dictionary terug die elk invoerwoord koppelt aan het aantal tekens. Herhaalde woorden hebben maar één entry nodig.",
  "word_lengths(words)",
  "return {word: len(word) for word in words}",
  "word_lengths(['map','torch','map']) == {'map':3,'torch':5} and word_lengths([]) == {}",
  'print(word_lengths(["map", "torch"]))',
);
C(
  ch,
  "mapping-build",
  2,
  "Frequency table",
  "Frequentietabel",
  "Count each label and return a dictionary of frequencies.",
  "Tel elk label en geef een dictionary met frequenties terug.",
  "frequencies(labels)",
  "counts = {}\nfor label in labels:\n    counts[label] = counts.get(label, 0) + 1\nreturn counts",
  "frequencies(['a','b','a']) == {'a':2,'b':1} and frequencies([]) == {}",
  'print(frequencies(["a", "b", "a"]))',
);
C(
  ch,
  "mapping-build",
  3,
  "Unique values",
  "Unieke values",
  "Return how many distinct values appear in the mapping. Values are strings or numbers.",
  "Geef terug hoeveel verschillende values in de mapping voorkomen. De values zijn strings of getallen.",
  "unique_values(mapping)",
  "seen = []\nfor value in mapping.values():\n    if value not in seen:\n        seen.append(value)\nreturn len(seen)",
  "unique_values({'a':2,'b':2,'c':3}) == 2 and unique_values({}) == 0",
  'print(unique_values({"a": 2, "b": 2, "c": 3}))',
);
C(
  ch,
  "mapping-build",
  4,
  "Group by initial",
  "Groeperen op beginletter",
  "Return a dictionary mapping each lowercase initial to a list of words with that initial. Ignore empty strings and preserve input order.",
  "Geef een dictionary terug met per beginletter in kleine letters een list van woorden. Negeer lege strings en behoud de invoervolgorde.",
  "group_initials(words)",
  "groups = {}\nfor word in words:\n    if not word:\n        continue\n    initial = word[0].lower()\n    if initial not in groups:\n        groups[initial] = []\n    groups[initial].append(word)\nreturn groups",
  "group_initials(['Oak','olive','','Birch']) == {'o':['Oak','olive'],'b':['Birch']} and group_initials([]) == {}",
  'print(group_initials(["Oak", "olive", "Birch"]))',
);
C(
  ch,
  "objects",
  1,
  "Build a beacon",
  "Een beacon maken",
  "A factory function can define a class and return an instance. Inside make_beacon, define Beacon with a name attribute and return a new instance for the supplied name.",
  "Een factory function kan een class definiëren en een instance teruggeven. Definieer binnen make_beacon een Beacon met een name-attribute en geef een nieuwe instance met de opgegeven naam terug.",
  "make_beacon(name)",
  "class Beacon:\n    def __init__(self, name):\n        self.name = name\nreturn Beacon(name)",
  "make_beacon('East').name == 'East' and make_beacon('West').name == 'West' and make_beacon('X') is not make_beacon('X')",
  'print(make_beacon("East").name)',
);
C(
  ch,
  "objects",
  2,
  "Add a switch",
  "Een schakelaar toevoegen",
  "Return a Beacon instance with name, active initially False, and turn_on() that sets active to True. Define the class inside the factory.",
  "Geef een Beacon-instance terug met name, active aanvankelijk False en turn_on() die active op True zet. Definieer de class binnen de factory.",
  "switched_beacon(name)",
  "class Beacon:\n    def __init__(self, name):\n        self.name = name\n        self.active = False\n    def turn_on(self):\n        self.active = True\nreturn Beacon(name)",
  "switched_beacon('E').active is False and (lambda b: (b.turn_on(),b.active)[1])(switched_beacon('E')) is True",
  'b = switched_beacon("East")\nb.turn_on()\nprint(b.active)',
);
C(
  ch,
  "objects",
  3,
  "Configurable alarm",
  "Instelbaar alarm",
  "Return a Sensor instance with name and threshold attributes. Its is_high(value) method returns True only above that threshold.",
  "Geef een Sensor-instance terug met name en threshold. De is_high(value)-method geeft alleen True boven die threshold.",
  "make_sensor(name, threshold)",
  "class Sensor:\n    def __init__(self, name, threshold):\n        self.name = name\n        self.threshold = threshold\n    def is_high(self, value):\n        return value > self.threshold\nreturn Sensor(name, threshold)",
  "make_sensor('A',10).is_high(11) is True and make_sensor('A',10).is_high(10) is False and make_sensor('B',5).name == 'B'",
  'print(make_sensor("East", 20).is_high(23))',
);
C(
  ch,
  "objects",
  4,
  "Independent counters",
  "Onafhankelijke tellers",
  "Return one Counter instance per name. Each starts with count 0; tick() increments only that instance’s count.",
  "Geef één Counter-instance per naam terug. Elke instance begint met count 0; tick() verhoogt alleen de eigen count.",
  "make_counters(names)",
  "class Counter:\n    def __init__(self, name):\n        self.name = name\n        self.count = 0\n    def tick(self):\n        self.count += 1\nreturn [Counter(name) for name in names]",
  "(lambda cs: (cs[0].tick(),[c.count for c in cs])[1])(make_counters(['A','B'])) == [1,0] and make_counters([]) == []",
  'counters = make_counters(["East", "West"])\ncounters[0].tick()\nprint([c.count for c in counters])',
);
C(
  ch,
  "objects",
  5,
  "Readable device",
  "Een leesbaar device",
  "Return a Device instance whose repr is Device(name, level). Include the actual name and level, without quotes around the name.",
  "Geef een Device-instance terug met repr Device(name, level). Gebruik de echte naam en het level, zonder aanhalingstekens rond de naam.",
  "make_device(name, level)",
  'class Device:\n    def __init__(self, name, level):\n        self.name = name\n        self.level = level\n    def __repr__(self):\n        return "Device({}, {})".format(self.name, self.level)\nreturn Device(name, level)',
  "repr(make_device('East',3)) == 'Device(East, 3)' and repr(make_device('West',8)) == 'Device(West, 8)'",
  'print(make_device("East", 3))',
);
