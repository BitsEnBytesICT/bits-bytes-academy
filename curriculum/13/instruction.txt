Hello World
Plus Equals
5 min

Python offers a shorthand for updating
variables
Preview: Docs Loading link description
. When we have a number saved in a variable and want to add to the current value of the variable, we can use the +=
(plus-equals) operator
Preview: Docs Perform operations on variables and values using special symbols and keywords in Python.
.

# First we have a variable with a number saved
number_of_miles_hiked = 12

# Then we need to update that variable
# Let's say we hike another two miles today
number_of_miles_hiked += 2

# The new value is the old value
# Plus the number after the plus-equals
print(number_of_miles_hiked)
# Prints 14

Copy to Clipboard

In this example, we keep a running count of how many miles a person has hiked over time. Instead of recalculating
from
Preview: Docs Loading link description
scratch, we keep a grand total and update it as we go hiking farther.

The plus-equals operator can also be used for string concatenation, like so:

hike_caption = "What an amazing time to walk through nature!"

# Almost forgot the hashtags!
hike_caption += " #nofilter"
hike_caption += " #blessed"

Copy to Clipboard

We create the social media caption for the nature photograph we took on our hike, then update it to include important social media tags that we initially forgot.
Instructions

    Checkpoint 1 Passed

    1.

    You’re doing a little bit of online shopping and find a pair of new sneakers. Right before checking out, you spot a nice sweater and some fun books you also want to purchase!

    Use the += operator to update total_price so that it includes the prices of nice_sweater and fun_books.

    The prices (also included in the workspace) are:
        new_sneakers = 50.00
        nice_sweater = 39.00
        fun_books = 20.00
