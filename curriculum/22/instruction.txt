Control Flow
Boolean Operators: not
11 min

The final Boolean operator we will cover is not. This operator is straightforward: when applied to any Boolean expression, it reverses the Boolean value. So if we have a True statement and apply a not operator, we get a False statement.

not True == False
not False == True

Copy to Clipboard

Consider the following statement:

Oranges are not fruits.

Here, we took the True statement oranges are fruits and added a not operator to make a False statement: oranges are not fruits.

This example in English is slightly different
from
Preview: Docs Loading link description
how it would appear in Python because in Python, we add the not operator at the beginning of the statement. Let’s take a look at some of those:

not 1 + 1 == 2  # False
not 7 < 0       # True

Copy to Clipboard

Instructions

    Checkpoint 1 Passed

    1.

    Set the variables statement_one and statement_two equal to the results of the following Boolean expressions:

    Statement one:

    not (4 + 5 <= 9)

    Copy to Clipboard

    Statement two:

    not (8 * 2) != 20 - 4

    Copy to Clipboard

    Checkpoint 2 Passed

    2.

    The registrar’s office at Calvin Coolidge’s Cool College has been so impressed with your work so far that they have another task for you.

    They want you to write a series of if statements using and and not statements:

        If a student does not have greater than or equal to 120 credits, print the following:

        "You do not have enough credits to graduate."

        Copy to Clipboard

        If their gpa is not greater than or equal to 2.0, print the following:

        "Your GPA is not high enough to graduate."

        Copy to Clipboard

        If they do not have greater than or equal to 120 credits and their gpa is not greater than or equal to 2.0, print the following:

        "You do not meet either requirement to graduate!"
