import { challenge as C } from "./helpers.mjs";
const ch = 6;
C(
  ch,
  "decisions",
  1,
  "Capacity check",
  "Capaciteit controleren",
  "Return True when used is at most capacity. Include the equality boundary.",
  "Geef True terug als used hoogstens capacity is. Neem de grenswaarde mee.",
  "fits(used, capacity)",
  "return used <= capacity",
  "fits(8,10) is True and fits(10,10) is True and fits(11,10) is False",
  "print(fits(8, 10))",
);
C(
  ch,
  "decisions",
  2,
  "Departure readiness",
  "Klaar voor vertrek",
  "Return True only when fuel is at least 20 and passengers is at most 8.",
  "Geef alleen True terug als fuel minstens 20 is en passengers hoogstens 8 is.",
  "ready(fuel, passengers)",
  "return fuel >= 20 and passengers <= 8",
  "ready(20,8) is True and ready(19,8) is False and ready(40,9) is False",
  "print(ready(25, 6))",
);
C(
  ch,
  "decisions",
  3,
  "Square threshold",
  "Grens voor een kwadraat",
  "Return whether the square of side exceeds limit. Equality should return False.",
  "Geef terug of het kwadraat van side groter is dan limit. Gelijkheid geeft False.",
  "exceeds(side, limit)",
  "return side ** 2 > limit",
  "exceeds(5,24) is True and exceeds(5,25) is False and exceeds(2,8) is False",
  "print(exceeds(5, 24))",
);
C(
  ch,
  "decisions",
  4,
  "Compare measurements",
  "Metingen vergelijken",
  "Return True when reading is between half of baseline and twice baseline, inclusive. Assume a positive baseline.",
  "Geef True als reading tussen de helft en het dubbele van baseline ligt, inclusief grenzen. Neem een positieve baseline aan.",
  "near_baseline(reading, baseline)",
  "return baseline / 2 <= reading <= baseline * 2",
  "near_baseline(5,10) is True and near_baseline(20,10) is True and near_baseline(21,10) is False",
  "print(near_baseline(12, 10))",
);
C(
  ch,
  "decisions",
  5,
  "Packing in rows",
  "Verpakken in rijen",
  "Return whether count can be split into rows of width with no remainder. Assume width is positive.",
  "Geef terug of count zonder rest kan worden verdeeld in rijen met width. Neem een positieve width aan.",
  "whole_rows(count, width)",
  "return count % width == 0",
  "whole_rows(24,6) is True and whole_rows(25,6) is False and whole_rows(0,4) is True",
  "print(whole_rows(24, 6))",
);
C(
  ch,
  "compound",
  1,
  "Open interval",
  "Open interval",
  "Return True only when value lies strictly between low and high. Exclude both endpoints.",
  "Geef alleen True terug als value strikt tussen low en high ligt. Sluit beide grenzen uit.",
  "inside(value, low, high)",
  "return low < value < high",
  "inside(5,2,8) is True and inside(2,2,8) is False and inside(8,2,8) is False",
  "print(inside(5, 2, 8))",
);
C(
  ch,
  "compound",
  2,
  "Matching identifiers",
  "Gelijke identifiers",
  "Return matched when the two identifiers are equal and different otherwise. Keep the exact lowercase output words.",
  "Geef matched als beide identifiers gelijk zijn en anders different. Gebruik precies deze kleine letters.",
  "compare_ids(left, right)",
  'if left == right:\n    return "matched"\nreturn "different"',
  "compare_ids('A1','A1') == 'matched' and compare_ids('A1','a1') == 'different'",
  'print(compare_ids("A1", "A1"))',
);
C(
  ch,
  "compound",
  3,
  "Spot the contradiction",
  "Een tegenspraak herkennen",
  "Return the result of testing whether a number is both below zero and above ten. Explain to yourself why no input can pass.",
  "Geef het resultaat van de test of een getal tegelijk onder nul en boven tien is. Bedenk waarom geen enkele invoer kan voldoen.",
  "contradiction(number)",
  "return number < 0 and number > 10",
  "all(contradiction(n) is False for n in [-20,0,5,20])",
  "print(contradiction(4))",
);
C(
  ch,
  "compound",
  4,
  "Signal strength",
  "Signaalsterkte",
  "Classify strength as weak below 30, fair from 30 through 69, and strong from 70 upward.",
  "Classificeer strength als weak onder 30, fair van 30 tot en met 69 en strong vanaf 70.",
  "signal_label(strength)",
  'if strength < 30:\n    return "weak"\nelif strength < 70:\n    return "fair"\nreturn "strong"',
  "signal_label(29) == 'weak' and signal_label(30) == 'fair' and signal_label(69) == 'fair' and signal_label(70) == 'strong'",
  "print(signal_label(64))",
);
C(
  ch,
  "compound",
  5,
  "Highest reading",
  "Hoogste meting",
  "Return the largest of three readings. If two readings share the maximum, still return that numeric maximum.",
  "Geef de grootste van drie metingen terug. Als twee metingen het maximum delen, geef je nog steeds dat numerieke maximum.",
  "highest(a, b, c)",
  "if a >= b and a >= c:\n    return a\nif b >= a and b >= c:\n    return b\nreturn c",
  "highest(2,8,5) == 8 and highest(8,8,1) == 8 and highest(-3,-7,-4) == -3",
  "print(highest(2, 8, 5))",
);
C(
  ch,
  "lists",
  1,
  "Add a summary",
  "Een samenvatting toevoegen",
  "Return a new list containing all input values followed by their count. Leave the input unchanged.",
  "Geef een nieuwe list terug met alle invoerwaarden, gevolgd door hun aantal. Laat de invoer ongewijzigd.",
  "with_count(values)",
  "return values + [len(values)]",
  "with_count([4,7]) == [4,7,2] and with_count([]) == [0] and (lambda x: (with_count(x),x)[1])([1,2]) == [1,2]",
  "print(with_count([4, 7]))",
);
C(
  ch,
  "lists",
  2,
  "Extend a pattern",
  "Een patroon uitbreiden",
  "For a list with at least two numbers, return a new list with one extra value equal to the sum of its final two numbers.",
  "Geef voor een list met minstens twee getallen een nieuwe list terug met één extra waarde: de som van de laatste twee getallen.",
  "extend_once(values)",
  "return values + [values[-2] + values[-1]]",
  "extend_once([2,3]) == [2,3,5] and extend_once([1,4,8]) == [1,4,8,12]",
  "print(extend_once([2, 3]))",
);
C(
  ch,
  "lists",
  3,
  "Shorter route",
  "Kortere route",
  "Return the shorter list. If lengths are equal, return the first list.",
  "Geef de kortste list terug. Zijn de lengtes gelijk, geef dan de eerste list terug.",
  "shorter(first, second)",
  "if len(first) <= len(second):\n    return first\nreturn second",
  "shorter([1],[2,3]) == [1] and shorter([1,2],[3]) == [3] and shorter([1],[2]) == [1]",
  "print(shorter([1, 2], [3]))",
);
C(
  ch,
  "lists",
  4,
  "Enough occurrences",
  "Voldoende voorkomens",
  "Return True when value occurs at least required times in values.",
  "Geef True terug als value minstens required keer voorkomt in values.",
  "enough(values, value, required)",
  "return values.count(value) >= required",
  "enough([1,2,1],1,2) is True and enough([1,2,1],1,3) is False and enough([],3,0) is True",
  "print(enough([1, 2, 1], 1, 2))",
);
C(
  ch,
  "lists",
  5,
  "Merge rankings",
  "Ranglijsten samenvoegen",
  "Return a descending sorted list containing both input lists. Preserve the original lists.",
  "Geef beide invoerlists samen terug als aflopend gesorteerde list. Behoud de oorspronkelijke lists.",
  "merge_ranked(left, right)",
  "return sorted(left + right, reverse=True)",
  "merge_ranked([2,8],[5,1]) == [8,5,2,1] and merge_ranked([],[]) == []",
  "print(merge_ranked([2, 8], [5, 1]))",
);
C(
  ch,
  "loops",
  1,
  "Count even readings",
  "Even metingen tellen",
  "Count how many numbers in values are even and return that count. Include zero.",
  "Tel hoeveel getallen in values even zijn en geef het aantal terug. Nul telt mee.",
  "count_even(values)",
  "count = 0\nfor value in values:\n    if value % 2 == 0:\n        count += 1\nreturn count",
  "count_even([0,1,2,4]) == 3 and count_even([]) == 0 and count_even([-2,-1]) == 1",
  "print(count_even([0, 1, 2, 4]))",
);
C(
  ch,
  "loops",
  2,
  "Build labels",
  "Labels maken",
  "Return a list containing Sensor: followed by a space and each name, preserving input order.",
  "Geef een list terug met Sensor: gevolgd door een spatie en elke naam, in dezelfde volgorde.",
  "sensor_labels(names)",
  'result = []\nfor name in names:\n    result.append("Sensor: " + name)\nreturn result',
  "sensor_labels(['East','West']) == ['Sensor: East','Sensor: West'] and sensor_labels([]) == []",
  'print(sensor_labels(["East", "West"]))',
);
C(
  ch,
  "loops",
  3,
  "Trim the beginning",
  "Het begin inkorten",
  "Return a new list with all leading zeros removed. Keep zeros after the first nonzero value.",
  "Geef een nieuwe list terug zonder nullen aan het begin. Behoud nullen na de eerste niet-nulwaarde.",
  "trim_zeros(values)",
  "index = 0\nwhile index < len(values) and values[index] == 0:\n    index += 1\nreturn values[index:]",
  "trim_zeros([0,0,3,0]) == [3,0] and trim_zeros([0,0]) == [] and trim_zeros([]) == []",
  "print(trim_zeros([0, 0, 3, 0]))",
);
C(
  ch,
  "loops",
  4,
  "Alternate samples",
  "Afwisselende samples",
  "Return values at indexes 0, 2, 4, and so on. Use a loop or a slice.",
  "Geef de waarden op indexes 0, 2, 4 enzovoort terug. Gebruik een loop of slice.",
  "alternate(values)",
  "result = []\nfor index in range(0, len(values), 2):\n    result.append(values[index])\nreturn result",
  "alternate([5,6,7,8,9]) == [5,7,9] and alternate([]) == []",
  "print(alternate([5, 6, 7, 8, 9]))",
);
C(
  ch,
  "loops",
  5,
  "Pairwise products",
  "Producten per paar",
  "Return every product of a value from left and a value from right, visiting left values first in order.",
  "Geef elk product van een waarde uit left en een waarde uit right terug. Doorloop eerst de left-waarden in volgorde.",
  "products(left, right)",
  "result = []\nfor a in left:\n    for b in right:\n        result.append(a * b)\nreturn result",
  "products([2,3],[4,5]) == [8,10,12,15] and products([], [1]) == []",
  "print(products([2, 3], [4, 5]))",
);
C(
  ch,
  "search",
  1,
  "Compare totals",
  "Totalen vergelijken",
  "Return the sum of whichever list has the smaller total. On a tie, the shared total is the answer.",
  "Geef de som terug van de list met het kleinste totaal. Bij gelijkheid geef je dat gedeelde totaal.",
  "smaller_total(left, right)",
  "left_total = 0\nright_total = 0\nfor value in left:\n    left_total += value\nfor value in right:\n    right_total += value\nreturn min(left_total, right_total)",
  "smaller_total([2,3],[9]) == 5 and smaller_total([],[]) == 0 and smaller_total([-2],[1]) == -2",
  "print(smaller_total([2, 3], [9]))",
);
C(
  ch,
  "search",
  2,
  "Stop at the budget",
  "Stoppen bij een budget",
  "Accumulate positive values until the total reaches or exceeds limit. Return the consumed total, or the full sum if the limit is never reached.",
  "Tel positieve waarden op tot het totaal limit bereikt of overschrijdt. Geef dat totaal terug, of de hele som als de grens niet wordt bereikt.",
  "until_limit(values, limit)",
  "total = 0\nfor value in values:\n    total += value\n    if total >= limit:\n        break\nreturn total",
  "until_limit([3,4,8],7) == 7 and until_limit([3,4,8],6) == 7 and until_limit([2],9) == 2 and until_limit([],2) == 0",
  "print(until_limit([3, 4, 8], 7))",
);
C(
  ch,
  "search",
  3,
  "Running minimum",
  "Lopend minimum",
  "Return the smallest number in a nonempty list by keeping a running candidate.",
  "Geef het kleinste getal in een niet-lege list terug door een lopende kandidaat bij te houden.",
  "smallest(values)",
  "candidate = values[0]\nfor value in values:\n    if value < candidate:\n        candidate = value\nreturn candidate",
  "smallest([4,1,8]) == 1 and smallest([-3,-8,-1]) == -8 and smallest([5]) == 5",
  "print(smallest([4, 1, 8]))",
);
C(
  ch,
  "search",
  4,
  "Matching positions",
  "Overeenkomende posities",
  "For equal-length lists, return indexes where the values match.",
  "Geef voor even lange lists de indexes terug waarop de waarden gelijk zijn.",
  "matching_positions(left, right)",
  "result = []\nfor i in range(len(left)):\n    if left[i] == right[i]:\n        result.append(i)\nreturn result",
  "matching_positions([1,2,3],[1,7,3]) == [0,2] and matching_positions([],[]) == []",
  "print(matching_positions([1, 2, 3], [1, 7, 3]))",
);
C(
  ch,
  "search",
  5,
  "Mirror order",
  "Omgekeerde volgorde",
  "Return True when right contains exactly the values of left in reverse order. Different lengths must return False.",
  "Geef True als right precies de waarden van left in omgekeerde volgorde bevat. Verschillende lengtes geven False.",
  "mirrors(left, right)",
  "if len(left) != len(right):\n    return False\nfor i in range(len(left)):\n    if left[i] != right[-1 - i]:\n        return False\nreturn True",
  "mirrors([1,2],[2,1]) is True and mirrors([1,2],[1,2]) is False and mirrors([],[1]) is False and mirrors([],[]) is True",
  "print(mirrors([1, 2], [2, 1]))",
);
C(
  ch,
  "numeric",
  1,
  "Cube a value",
  "Een waarde tot de derde macht",
  "Return the cube of number. Negative inputs should keep the correct sign.",
  "Geef number tot de derde macht terug. Negatieve invoer moet het juiste teken behouden.",
  "cube(number)",
  "return number ** 3",
  "cube(3) == 27 and cube(-2) == -8 and cube(0) == 0",
  "print(cube(3))",
);
C(
  ch,
  "numeric",
  2,
  "Diagonal length",
  "Lengte van een diagonaal",
  "Return the diagonal of a rectangle using (width² + height²) to the power 0.5.",
  "Geef de diagonaal van een rechthoek terug met (width² + height²) tot de macht 0.5.",
  "diagonal(width, height)",
  "return (width ** 2 + height ** 2) ** 0.5",
  "diagonal(3,4) == 5 and diagonal(0,7) == 7",
  "print(diagonal(3, 4))",
);
C(
  ch,
  "numeric",
  3,
  "Completion percentage",
  "Voltooiingspercentage",
  "Return completed / total * 100. Return 0 when total is zero.",
  "Geef completed / total * 100 terug. Geef 0 als total nul is.",
  "percentage(completed, total)",
  "if total == 0:\n    return 0\nreturn completed / total * 100",
  "percentage(3,4) == 75 and percentage(0,0) == 0 and percentage(2,2) == 100",
  "print(percentage(3, 4))",
);
C(
  ch,
  "numeric",
  4,
  "Weighted reading",
  "Gewogen meting",
  "Return an average in which recent has twice the weight of previous: divide previous + 2 * recent by 3.",
  "Geef een gemiddelde waarin recent dubbel meetelt: deel previous + 2 * recent door 3.",
  "weighted(previous, recent)",
  "return (previous + 2 * recent) / 3",
  "weighted(3,6) == 5 and weighted(9,0) == 3 and weighted(4,4) == 4",
  "print(weighted(3, 6))",
);
C(
  ch,
  "numeric",
  5,
  "Clock wrap",
  "Terug naar het begin van de klok",
  "Return the hour after adding elapsed to start on a 24-hour clock.",
  "Geef het uur na elapsed uur vanaf start op een 24-uursklok terug.",
  "clock_hour(start, elapsed)",
  "return (start + elapsed) % 24",
  "clock_hour(22,5) == 3 and clock_hour(8,48) == 8 and clock_hour(0,0) == 0",
  "print(clock_hour(22, 5))",
);
C(
  ch,
  "compose",
  1,
  "Scale three ways",
  "Drie schalen",
  "Return a tuple containing number multiplied by 2, 4, and 8, in that order.",
  "Geef een tuple terug met number maal 2, 4 en 8, in die volgorde.",
  "scales(number)",
  "return number * 2, number * 4, number * 8",
  "scales(3) == (6,12,24) and scales(-1) == (-2,-4,-8)",
  "print(scales(3))",
);
C(
  ch,
  "compose",
  2,
  "Apply a discount",
  "Korting toepassen",
  "Return the price after discount_percent percent is removed.",
  "Geef de prijs terug nadat discount_percent procent is afgetrokken.",
  "discounted(price, discount_percent)",
  "return price * (1 - discount_percent / 100)",
  "discounted(80,25) == 60 and discounted(15,0) == 15 and discounted(20,100) == 0",
  "print(discounted(80, 25))",
);
C(
  ch,
  "compose",
  3,
  "Build a tag",
  "Een tag maken",
  "Return a label formatted as category + colon + space + name.",
  "Geef een label terug als category + dubbele punt + spatie + name.",
  "tag(category, name)",
  'return category + ": " + name',
  "tag('Sensor','East') == 'Sensor: East' and tag('Route','Blue') == 'Route: Blue'",
  'print(tag("Sensor", "East"))',
);
C(
  ch,
  "compose",
  4,
  "Convert minutes",
  "Minuten omrekenen",
  "Return a tuple of whole hours and remaining minutes for a nonnegative number of minutes.",
  "Geef voor een niet-negatief aantal minuten een tuple terug met hele uren en resterende minuten.",
  "hours_minutes(minutes)",
  "return minutes // 60, minutes % 60",
  "hours_minutes(135) == (2,15) and hours_minutes(60) == (1,0) and hours_minutes(0) == (0,0)",
  "print(hours_minutes(135))",
);
C(
  ch,
  "compose",
  5,
  "Measurement summary",
  "Samenvatting van metingen",
  "Return a tuple containing the sum, product, and absolute difference of a and b.",
  "Geef een tuple terug met de som, het product en het absolute verschil van a en b.",
  "summary(a, b)",
  "return a + b, a * b, abs(a - b)",
  "summary(3,7) == (10,21,4) and summary(-2,5) == (3,-10,7)",
  "print(summary(3, 7))",
);
